# frontend/.env  (CRA)
# Public configuration only — NOT secrets

# When developing locally keep the backend at localhost:
REACT_APP_API_URL=https://seminar-hall-backend-4noi.onrender.com

# Optional flags
REACT_APP_ENV=development
REACT_APP_DEBUG=true
